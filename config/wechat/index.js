/**
 * @author Flower
 * @description wechat configuration
 */
var httpService = require( './service' )
var config = require( './wechat.config' )
module.exports = {
  httpservice: httpService,
  config: config
}
